# [Product Name]

[One line: what it is, who it's for.]

## Stack
- [e.g. Next.js / React, Supabase, Claude API]

## Run locally
1. `npm install`
2. Copy `.env.example` to `.env` and fill in the values
3. `npm run dev`

## Folder map
- `app/` — routes
- `components/` — UI
- `lib/` — domain logic, data fetching, types (keep logic out of components)

## Notes
- Project rules live in `CLAUDE.md`
- Safety hook lives in `.claude/hooks/`
