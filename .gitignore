.DS_Store
node_modules/
.env
.env.*
!.env.example

# Playwright MCP session artifacts
.playwright-mcp/

# Next.js / TypeScript build artifacts
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Local scratch / reference data — kept locally, never committed.
# Screenshots live at repo root (real image assets go in public/ or src/).
# vod_titles.* is a client catalog export (Tier-3 data): add a fixture
# deliberately under a fixtures dir if the BYO-sheet mapping work needs one.
/*.png
/vod_*.csv
/vod_*.json
.vercel
.claude/worktrees/

# Superpowers SDD scratch workspace (briefs, per-task reports, diffs).
# The durable ledgers are copied into docs/superpowers/ledgers/ and tracked there.
.superpowers/
