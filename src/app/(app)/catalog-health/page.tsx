import { PageHeader } from "@/components/ui/page-header";

export default function CatalogHealthPage() {
  return (
    <PageHeader title="Catalog Health" subtitle="This section arrives with its build slice." />
  );
}
