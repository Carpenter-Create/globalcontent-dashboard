import { PageHeader } from "@/components/ui/page-header";

export default function DeliveriesPage() {
  return <PageHeader title="Deliveries" subtitle="This section arrives with its build slice." />;
}
